[commit-msg.commands]
cog-lint = { run = "cog verify --file {1}" }

[pre-commit.commands]
lint = { run = "make lint" }

[pre-push.commands]
lint = { run = "make lint" }
test = { run = "make test" }

[post-checkout.commands]
sync-branch = { run = "make sync-branch" }

[post-merge.commands]
sync-branch = { run = "make sync-branch" }
