version = "2"

[linters]
default = "fast"
enable = ["revive"]
settings = { revive = { enable-default-rules = true, rules = [
  { name = "package-comments", disabled = true },
] } }

[formatters]
enable = ["gofumpt"]
settings = { gofumpt = { extra-rules = true } }
